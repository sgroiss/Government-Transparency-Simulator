// Shared by the content script and the options page (same global scope in both).
// This list is the joke. Change it and you are just a text highlighter.
const DEFAULT_TERMS = ["Trump", "Epstein", "Maxwell"];
